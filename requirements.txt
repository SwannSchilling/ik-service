# ik_service runtime (Python 3.12).
# The pickik C++ binding is built with CMake (see README.md), not pip.
fastapi>=0.110
uvicorn>=0.29
numpy>=1.26
pybind11>=2.12

# test dependencies
pytest>=8
httpx>=0.27
